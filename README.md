# Current Clock

Tiny Flask app that displays the current time and auto-refreshes every second.

## Local Run

```bash
python -m venv .venv
# Windows
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python app.py
# App runs at http://127.0.0.1:5000/
